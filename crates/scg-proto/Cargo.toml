[package]
name = "scg-proto"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "gRPC/protobuf definitions for the Secure Communication Gateway management API."

[dependencies]
tonic = "0.12"
prost = "0.13"

[build-dependencies]
tonic-build = "0.12"
