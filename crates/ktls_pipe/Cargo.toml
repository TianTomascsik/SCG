[package]
name = "ktls_pipe"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Kernel TLS (kTLS) session primitives shared with the SCG gateway data plane."
publish = false

[dependencies]
openssl = "0.10"
openssl-sys = "0.9"
libc = "0.2"
foreign-types-shared = "0.1"
