[package]
name = "scg-ipc"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Shared IPC primitives for the Secure Communication Gateway and its client libraries: framed packets, sealed shared-memory rings, capability tokens, and Linux IPC syscall helpers."
publish = false

[dependencies]
libc = "0.2"

[features]
default = []
